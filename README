jQuery Validator Plugin Wrapper Class
=====================================

Usage:-
-------

$validator = new jQueryValidator();
$validator->setForm('#signup-form');
$validator->validateOnBlur(); // Optional.

// Assign a rule using format $validator->setRule(<element>, <rule>, <message>);
$validator->setRule("name", "required", "Please provide your name."); 

<script>
<?php echo $validator->getValidationCode(); ?>
</script>